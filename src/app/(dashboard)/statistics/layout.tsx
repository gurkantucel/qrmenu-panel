import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
    title: 'İstatistikler',
    description:
        "İstatistikleri listeler.",
};

export default Page;