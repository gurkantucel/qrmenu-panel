import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
    title: 'SMS Gönder',
    description:
        "Gönderilen SMSleri listeler.",
};

export default Page;